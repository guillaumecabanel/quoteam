.modal.fade id="new-quote" tabindex="-1" role="dialog"
  .modal-dialog role="document"
    .modal-content
      .modal-header
        button.close type="button" data-dismiss="modal" aria-label="Close"
          span aria-hidden="true" &times;
        h4.modal-title = t('.new')
      .modal-body
        = simple_form_for [@team, quote] do |f|
          = f.input :content
          = f.input :author 
          = f.button :button, t('.create').html_safe, class: 'btn btn-success',
                              data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> #{t('.saving')}" }
